{% extends 'base.html.twig' %}

{% block title %}Edit Planning{% endblock %}

{% block body %}


    <h1 class="text-dark text-center">Modifier Planning</h1>

    <div class="row d-flex justify-content-center">
        <div class="col-md-8">
            {{ include('planning/_form.html.twig', {'button_label': 'Update'}) }}

                {{ include('planning/_delete_form.html.twig') }}

        </div>

    </div>

    <div class="mt-3 text-center">
        <a class="btn btn-secondary" href="{{ path('app_planning_index') }}">Retour à la liste</a>
    </div>



{% endblock %}
